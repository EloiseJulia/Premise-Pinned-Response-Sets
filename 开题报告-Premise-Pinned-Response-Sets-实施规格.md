开题报告与实施规格：把评分不确定性从模型自报改为可执行构造

本文档同时是一份可交给编码 Agent 执行的实施规格。第四节至第八节的所有取值均已锁定，不得在实现阶段自行更改；确需变更时写入偏离日志。

# 一、背景与目标设定

本项目的目标不是投稿，而是在短周期内产出一组可信、可读、与目标实验室最新工作直接对话的实验结果，作为与 Kenneth Holstein 建立实质科研联系的基础。目标受众是一位已经回过一封邮件、对该领域极度熟悉、且时间极度有限的研究者。

这一目标设定直接产生三条与“为了发表而做”不同的设计取舍，实施时必须始终持守。

其一，可读性优先于完备性。最终产物是一张图加三句话，要能在三十秒内被看懂。所有实验设计都从这张图倒推；任何不服务于它的维度都剔除。

其二，矩阵尽量窄，重复尽量深。在不限 token 额度下，富余算力应当换成更稳的估计与更完整的消融，而不是更多的任务与模型。一个六模型八任务的 pilot 比一个四模型三任务的 pilot 更难解释，不是更容易。

其三，诚实度本身就是信号。论文 A 用大量篇幅自讨自己的假设不合现实，论文 D 的作者主动公开了自己捕获的三个静默 bug 并推翻了自己草稿里的一个伪结论。这个群体的认知风格是测量有效性优先。因此一份带有清晰边界声明的 pilot，比一份声称发现了什么的 pilot 更能证明成熟度。不要过度声称。

# 二、与目标工作的关联

## 2.1 论文 A 留下的缺口

A（Validating LLM-as-a-Judge Systems under Rating Indeterminacy, NeurIPS 2025）证明：当一个条目存在多个合理评分时，强制单选标注造成的信息损失是结构性不可恢复的，基于它的一致性指标会系统性选错 judge。它用单参数 β 刻画每个 judge 把多重解释塌缩到单选的偏好。

但 β 完全来自一个未验证的假设：直接提示 LLM 选出所有合理选项，得到的集合就是它的 response set。A 在 Future Work 第 8 条中把“LLM 自报的 response set 是否具有构念效度”列为未解问题，并标注其价值为“整个 β 测量的效度基础”、难度为低到中。本项目直接填这一栏。

A 的另一条自认局限是：11 个任务的人类 response set 分布从未被观测，全部由 β 扫描反向构造；而它的附录 E 又表明每条目 1 到 3 个标注时所有指标都失效、需要 5 到 10 个。本项目用 ChaosNLI（每条目 100 个人工标签）同时解决这两个问题。

## 2.2 我自己工作中可迁移的机制

论文 C 的可迁移内核是两句话：不确定性有两条正交的轴，而重采样只能看到其中一条；另一条可以用前提自曝加反事实钉住主动探测。C 在可执行任务上拿到 AUROC 0.895，而同期的 semantic entropy 只有 0.581——因为危险信号恰恰是“稳定”而不是“犹豫”。

把这个机制用到离散评分任务上时，C 最脆弱的依赖会自动消失：它要求存在可执行的确定性等价性判据，而在选项有限离散的评分任务中，等价性聚类退化为“评分标签相等”。聚类步骤因此是免费且确定性的，不需要任何 LLM 充当裁判。

论文 D 提供方法学骨架：预注册冻结、报分布不报点估计、阈值扫描与门控翻转率、措辞消融。这些直接搬用，使一个两周的小实验从第一天起就具备可审稿的形态。

## 2.3 核心缝隙

A 与 B 都把评分不确定性压缩成一个标量，都能回答“有多少不确定”，都不追问“不确定沿哪一条语义轴产生”。后果是：一个在重采样下完全稳定、但只是稳定地钉在某个未言明前提上的 judge，会被 A 的全部强制单选指标判为“没有不确定性”。本项目就是把这条轴接上去。

# 三、锁定的课题

## 3.1 研究问题

主 RQ：judge 的评分不确定性中，有多大比例来自未言明的评分前提（rubric 歧义），而非采样波动或提示词顺从性？

次 RQ：用前提钉住构造的 response set，是否比 LLM 自报的 response set 更接近实测的人类评分分布？

## 3.2 核心机制（四步）

第一步，自曝。用通用提示让 judge 列出“要评这一条，必须先固定哪些未言明的评分前提”。提示中绝不得出现任何选项词或金标提示。

第二步，钉住。把每个前提反事实钉到 judge 自己给出的候选值上，每个钉住条件下各跑一次强制单选。采用一次只钉一维的方式。

第三步，取并集。所有钉住条件下得到的评分标签取并集，即为 premise-pinned response set（下称 PPRS）；标签分布的香农熵即为上下文熵。

第四步，接入。把 PPRS 作为 A 的连续多标签聚合函数的输入，与 A 的自报版本及全部强制单选指标放在同一条选型与下游验证管线上比较。

## 3.3 预注册假设

H1：β_self 与 β_pin 的相关较弱（预注册预测 Pearson r 小于 0.4）。即 A 的敏感度参数中有相当一部分不是语义上的多重解释。

H2：存在非平凡的危险质量。一批条目上 judge 种子熵近于零但上下文熵显著大于零（预注册预测占比大于 15%）。

H3：PPRS 比自报 response set 更接近 ChaosNLI 实测的人类分布，且优势随任务的 rubric 模糊程度单调增强。

三条假设均为双向可报告。H1 不成立（即两者高度相关）是对 A 关键假设的首份外部效度证据，同样值得写进邮件。不存在“无结论”结局。

# 四、已锁定的设计决策

下表的每一行都是硬约束。实现阶段若发现某一条无法执行，先停下来记入偏离日志并确认，不要自行选一个替代方案继续。

| 决策项 | 锁定取值 | 理由 |
|----|----|----|
| 主课题 | Premise-Pinned Response Sets | 直接填 A 自己点名的 Future Work 第 8 条，沟通成本最低 |
| 前提钉住方式 | 一次只钉一维 | 与论文 C 一致，且 localization 指标只在单维下有定义；全网格降为子集消融 |
| 人类参照物 | ChaosNLI 实测分布 | 每条目 100 个人工标签，同时解决 A 的“人类分布未被观测”与“标注数不足”两条局限 |
| NLI 条目采样 | 必须从 ChaosNLI 的约 3100 条内抽取 | 从原始 SNLI/MNLI 随机抽则人类金标不存在，整个 H3 无法检验 |
| 危险象限方向 | 低种子熵且高上下文熵 | 高种子熵是现有方法已能抓到的可见不确定性，没有新意；弄反会丢失全部新意量 |
| 阈值处理 | π 与 τ 均全扫描，不取单值 | 采用论文 D 的阈值扫描规范，主结果报稳健性而非点估计 |
| 调用框架 | litellm 加 asyncio 加 Parquet 缓存，不引入 Inspect AI | 路径 P 的第二阶段调用次数依赖第一阶段输出，固定管线抽象在这里是负担 |
| 上游仓库引用 | git submodule 铉 commit，不复制代码 | 两个上游仓库均无许可证；本阶段不分发不发表，保持合规即可 |

# 五、实验规格

## 5.1 任务与数据

三个任务，构成 rubric 模糊度的梯度，这个梯度本身就是 H3 的检验维度。

| 任务 | 数据来源 | 每条目人工标注数 | rubric 模糊度与作用 |
|----|----|----|----|
| NLI-SNLI | ChaosNLI 的 SNLI 子集，150 条 | 100 | 低。选项语义相对硬，作为对照端；人类金标最强 |
| NLI-MNLI | ChaosNLI 的 MNLI 子集，150 条 | 100 | 低到中。跨 genre，用于检验结论是否只是 SNLI 特有 |
| SummEval-Relevance | SummEval，150 条 | 8（5 众包加 3 专家） | 高。relevant 指覆盖度还是简洁度未定义，预期前提敏感度最大 |

关于选项空间的强制要求：SummEval 是 1 到 5 李克特量表，而 A 的框架只支持离散选项。实现的第一件事是打开 lguerdan/indeterminacy 的 configs/tasks.py 与 configs/prompts.py，查明 A 对 SummEval 采用的离散化方式并原样沿用。不得自行另定一套——否则数字无法与 A 对拓，整个对比失去意义。查明后把结果写回本节。

## 5.2 模型名单

四个 judge，覆盖两个厂商与两个能力层，其中至少一个推理模型。包含弱模型是有理论动机的：论文 C 报告弱模型的过度澄清率显著高于推理模型（汇总 0.402 对 0.032），因此能力层与前提自曝质量之间预期存在交互，这是一个预设的子分析。

模型必须以快照版本字符串记录（例如带日期后缀的完整名），不得使用会隐式漂移的别名。论文 D 明确把跨时间快照漂移列为未解风险，我们用快照名提前堆掉它。

## 5.3 三条引出路径

三条路径在同一批条目、同一组模型上并行执行，结果落入同一个 schema。

路径 F（基线）：强制单选提示，每条目重采样 20 次，得到强制单选分布与种子熵。采样数从 A 的 10 提到 20，因为 A 的附录 E 表明低采样数时所有指标失效，而我们不受额度限制。

路径 S（A 的方案）：直接提示 judge 选出所有可能合理的选项，重采样 20 次，得到自报 response set 与 β_self。

路径 P（本项目）：两阶段。阶段一自曝前提列表，每个前提带 2 到 3 个候选值与一个类型标签（歧义 / 模糊 / 分歧）；阶段二对每个前提与候选值的组合各跑一次强制单选。阶段一也重复 3 次，用于评估前提自曝本身的稳定性——这是一个审稿人必问、且目前无人回答的问题。

全网格钉住（同时钉住多个前提）降级为子集消融，每任务 20 条，用于回答前提之间是否存在交互。不进主管线，因为交互项会让上下文熵失去单一解释。

## 5.4 必备消融

安慰剂提示。一个长度与句法与路径 P 匹配、但钉住的是与评分无关的维度的提示。用于把“多跑几次且提示变长”本身的效应与真正的前提敏感度分开。没有这一项，主结果无法辩护。

温度扫描。0 与 0.7 两档，用于区分共享先验与贪心解码两种收敛来源。

前提泄漏审计。由一个不在被测名单内的跨家族模型检查自曝提示是否隐含地提示了答案轴。抽检每任务 30 条。

选项顺序随机化。所有强制单选与多选提示的选项顺序必须随机化，并把排列种子记入原始记录。A 未控制选项顺序偏差，这是一个低成本的改进点。

# 六、指标定义

## 6.1 两种熵

种子熵 H_seed：对路径 F 的 20 次重采样，按任务、条目、judge 分组，对评分标签分布算香农熵。这是现有方法（self-consistency、semantic entropy）能看到的轴。

上下文熵 H_ctx：对路径 P 阶段二的全部钉住结果，按同样分组，先在候选值上边缘化再算熵。这是现有方法看不到的轴。

## 6.2 危险象限（方向不得弄反）

危险象限定义为 H_ctx 大于 0 且 H_seed 不超过 0.5 比特，即低种子熵且高上下文熵——模型在重采样下完全稳定、看上去自信，但一旦改变它自己默认的前提就翻。

高种子熵的条目不是危险象限。那是可见的不确定性，现有方法本来就能抓到，没有新意。本项目的全部新意量建立在“自信、稳定、且错”这个象限上。这一条写在这里是因为它极容易在实现时被弄反，而一旦弄反，图表看起来一切正常。

## 6.3 人类 response set 的导出

ChaosNLI 每条目有 100 个人工标签。一个选项被纳入人类 response set，当且仅当选择它的人占比不低于阈值 π。

π 必须全扫描，取值 0.05 到 0.25，步长 0.05。不得拍一个值。主结果报告的是“结论在整个 π 带上是否稳健”，而非某个 π 下的点估计。同理，A 的阈值 τ 也全扫描。这是论文 D 的阈值扫描方法学在新场景的应用。

## 6.4 主要输出

图一，两种敏感度参数的散点图。横轴 β_self，纵轴 β_pin，每点为一个任务与 judge 的组合，标出相关系数与对角线。这是 H1 的直接检验，也是邮件里要放的那张图。

图二，两种熵的二维分布。横轴 H_seed，纵轴 H_ctx，每点为一个任务、条目、judge 的组合；左上危险象限加阴影并标出占比，另导出高危条目清单。这是 H2 的检验。

图三，各指标选出的首位 judge 与实测人类分布的距离。横轴为候选指标（Hit Rate、两个方向的 KL、Coverage、MSE_self、MSE_pin），纵轴为该指标选出的首位 judge 相对最优的差距。这是 H3 的检验。

# 七、结果 schema 与工程红线

## 7.1 不可违反的三条

第一，解析失败必须显式记录。每条记录带一个 parse_status 字段，取值为 ok、malformed_json、missing_field、refused、timeout、provider_error 之一；失败时相应的结果字段严格置空。绝不允许用众数、均值或任何默认值静默填充。论文 D 的预注册自检管线捕获的三个静默 bug 中，最危险的一个就是解析器在失败时伪造哨兵值。这一条必须有一个专门的反例测试并进入 CI 必过项。

第二，幂等缓存。缓存键为渲染后提示、模型快照、温度、top_p、种子、响应格式的哈希；任一字段变化即视为新调用。每次调用完成立即追写原始记录，支持断点续跑。

第三，合规处理。lguerdan/indeterminacy 与 lguerdan/doubly-robust-llm-judge 两个仓库均未标注许可证。用 git submodule 铉住具体 commit 引用，不得把其代码复制进本仓库，README 写明来源。本阶段为私下复现与学习，不分发、不发表；若后续转为投稿，届时再向作者征询使用授权。

## 7.2 结果表必备字段

存为 Parquet。字段分为标识、调用参数、原始与解析结果、计量四组，完整列表见下表。推导字段（两种熵、PPRS 多热向量、敏感度参数）在分析层生成，不入原始表。

| 字段组 | 字段 | 说明 |
|----|----|----|
| 标识 | cache_key, run_tag, git_sha, prereg_tag | cache_key 为主键；prereg_tag 对应预注册冻结的 git tag |
| 实验坐标 | task, item_id, judge_id, path, sample_id | path 取值为 forced_choice / multi_label / premise_pinned / placebo |
| 前提坐标 | premise_id, premise_type, premise_value, premise_round | premise_type 取歧义 / 模糊 / 分歧；premise_round 记录自曝的第几次重复 |
| 调用参数 | model_snapshot, provider, temperature, top_p, seed, prompt_template_id, prompt_hash, option_permutation_seed | 必须记快照名而非别名；选项排列种子用于位置偏差审计 |
| 原始结果 | raw_text | 原始响应全文，任何情况下都保留 |
| 解析结果 | parsed_choice_hard, parsed_choice_set, parsed_premises | 失败时严格置空，不得填默认值 |
| 状态 | parse_status, provider_error, http_status, retry_count | parse_status 是分析层筛选的唯一依据 |
| 计量 | prompt_tokens, completion_tokens, wall_clock_ms, ts_utc | 不需要成本字段，但保留 token 计数用于报告规模 |
| 推导（不入原表） | H_seed, H_ctx, PPRS 多热向量, β_self, β_pin | 在分析层由原表重算，保证可从原始数据重建 |

# 八、实施顺序与验收标准

交给编码 Agent 时，按下表顺序推进。每一阶段都有一个可机械判定的验收标准，未达标不得进入下一阶段。特别注意：全量跑在第六阶段才开始，前五个阶段的全部目的是把不确定性从实验里挤出去。

| 阶段 | 内容 | 验收标准（未达标不得进入下一阶段） |
|----|----|----|
| 一 | 查明 A 对 SummEval 的离散化方式；建仓库骨架与依赖锁定 | 三个任务的选项集已写入 configs 且与 A 一致；目录树建成 |
| 二 | 异步调用层、幂等缓存、解析器、mock provider | 两个测试全绿：缓存键幂等性；构造的畔形 JSON、缺字段、超时三种情形下 parse_status 均非 ok 且结果字段为空 |
| 三 | 前提自曝提示迭代，20 条目 × 2 模型 × 4 至 5 个措辞变体 | 人工阅读全部自曝输出，绝大多数条目产出的是可钉住的评分前提，而非任务复述或泛泛的方法论 |
| 四 | 预注册冻结并打 tag | 三条假设、两个阈值的扫描范围、危险象限方向、主图形式均已写死并提交 |
| 五 | 烟雾测试，10 条目 × 2 模型 × 三路径 | 解析成功率不低于 95%；第二次运行缓存命中率 100%；人工抽查 20 条确认无静默填充 |
| 六 | 全量跑与分析流水线，输出三张图 | 三张图可从原始 Parquet 一键重建；结果哈希已提交 |

关于第三阶段的额外说明：前提自曝提示没有任何现成模板可用，它是整个项目的核心资产，必须靠迭代打磨。判定标准只有一条：自曝出来的是不是真正的评分前提，而不是任务复述、注意事项或泛泛的方法论。弱模型上的过度澄清是已知会出现的现象，不要因此就把提示改得很限制性。

# 九、两周交付物与邮件骨架

## 9.1 交付物

一个可复现的仓库（含预注册冻结提交与偏离日志），三张图，以及一份不超过四页的技术简报。简报写成自包含的单文件，使它可以被直接转发——这一点在委员会制招生下尤其重要，因为真正读到它的人可能不只一位。

## 9.2 邮件骨架

邮件不要长。建议结构为四段：第一段一句话说明我读了哪两篇、并针对其中一条具体的 Future Work 做了一个 pilot；第二段放图一与一句结论；第三段主动写出这个 pilot 的边界（任务少、模型少、人类金标只在 NLI 侧存在）；第四段给仓库与四页简报链接。

第三段是最容易被省略、但回报最高的一段。对一位写了整篇论文论证“现行评测指标在测量有效性层面不成立”的研究者而言，一个能准确陈述自己结论边界的人，比一个声称自己发现了大结果的人可信得多。

另建议在邮件里简短提一句文献排查的结果：我原本还设计了一个关于多 judge 面板有效集成规模的子课题，检索后发现 2026 年 5 月已有论文在同一类数据上回答了它，于是把该分支重定位为“按前提正交性构造面板”。这一句传递的是文献敏锐度与自我修正能力，它比多一张图更有说服力。

# 十、已知风险与诚实边界

下列条目必须写进四页简报，不得隐去。

人类金标只在 NLI 侧存在。SummEval 侧每条目仅约 8 个标注，无法支撑与 ChaosNLI 同等强度的人类分布估计。因此 H3 在三个任务上的证据强度不相等，必须分开报告。

前提自曝的构念效度未被独立验证。模型声称的“前提”与人类实际依据的理由是否同一构念，本 pilot 不回答。若时间允许，可用 e-SNLI 的人类解释做一个小规模语义对齐作为初步证据，但不要把它当作已解决。

结论限于本批快照。商用模型版本漂移会直接改变两种敏感度参数的数值，因此只主张方向性结论，不主张具体数值可复现。

本项目不声称任何关于真实世界发生率的结论。它回答的是一种失效模式是否存在、能否被测量，而不是它在部署中有多普遍。
